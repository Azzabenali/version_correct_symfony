 <style>
        .btn-prestige-delete {
            width: 42px;
            height: 42px;
            background: #fff1f2;
            color: #be123c;
            border: 1px solid #fee2e2;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.2s ease;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 1rem;
        }

        .btn-prestige-delete:hover {
            background: #be123c;
            color: white;
            border-color: #be123c;
            transform: scale(1.05);
        }
    </style>

<form method="post" action="{{ path('app_lieu_delete', {'id': lieu.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément de prestige ?');" class="delete-container">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lieu.id) }}">
    <button class="btn-prestige-delete" title="Supprimer définitivement">
        <i class="fas fa-trash-alt"></i>
    </button>
</form>
