<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Prestige Admin - Gestion{% endblock %}</title>

    <!-- Polices Premium -->
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@600;700&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome pour les icônes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --gold-primary: #c5a059;
            --gold-secondary: #e2c275;
            --dark-bg: #0f172a;
            --dark-sidebar: #0a0f1d;
            --text-light: #f8fafc;
            --text-muted: #94a3b8;
            --accent-gold: #b48c3e;
            --sidebar-width: 280px;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f1f5f9;
            color: var(--dark-bg);
            display: flex;
            min-height: 100vh;
        }

        /* Barre latérale Prestige */
        .sidebar {
            width: var(--sidebar-width);
            background-color: var(--dark-sidebar);
            color: white;
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
            z-index: 100;
            box-shadow: 10px 0 30px rgba(0, 0, 0, 0.1);
        }

        .sidebar-header {
            padding: 3rem 2rem;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .sidebar-header h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.8rem;
            letter-spacing: 4px;
            color: var(--gold-primary);
            text-transform: uppercase;
        }

        .sidebar-nav {
            flex: 1;
            padding: 2rem 0;
            overflow-y: auto;
        }

        .sidebar-nav ul {
            list-style: none;
        }

        .sidebar-nav li {
            margin-bottom: 0.5rem;
        }

        .sidebar-nav a {
            display: flex;
            align-items: center;
            padding: 1rem 2rem;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            border-left: 4px solid transparent;
            gap: 1rem;
        }

        .sidebar-nav a i {
            width: 20px;
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .sidebar-nav a:hover, .sidebar-nav a.active {
            color: white;
            background: rgba(197, 160, 89, 0.05);
            border-left-color: var(--gold-primary);
        }

        .sidebar-nav a.active i, .sidebar-nav a:hover i {
            color: var(--gold-primary);
        }

        /* Contenu principal */
        .main-content {
            flex: 1;
            margin-left: var(--sidebar-width);
            display: flex;
            flex-direction: column;
            min-width: 0;
        }

        .header {
            background: white;
            padding: 1.5rem 3rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
            position: sticky;
            top: 0;
            z-index: 90;
        }

        .header h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            font-weight: 700;
            color: var(--dark-bg);
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 0.5rem 1.25rem;
            background: #f8fafc;
            border-radius: 50px;
            border: 1px solid #e2e8f0;
        }

        .user-avatar {
            width: 35px;
            height: 35px;
            background: var(--gold-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 0.8rem;
        }

        .user-name {
            font-size: 0.85rem;
            font-weight: 600;
            color: var(--dark-bg);
        }

        .content-wrapper {
            padding: 3rem;
            flex: 1;
        }

        /* Scrollbar personnalisée pour la sidebar */
        .sidebar-nav::-webkit-scrollbar {
            width: 5px;
        }
        .sidebar-nav::-webkit-scrollbar-track {
            background: var(--dark-sidebar);
        }
        .sidebar-nav::-webkit-scrollbar-thumb {
            background: var(--gold-primary);
            border-radius: 10px;
        }

        @media (max-width: 1024px) {
            .sidebar {
                width: 80px;
            }
            .sidebar-header h2, .sidebar-nav span {
                display: none;
            }
            .main-content {
                margin-left: 80px;
            }
            .sidebar-nav a {
                justify-content: center;
                padding: 1.5rem 0;
            }
            .sidebar-nav a i {
                margin: 0;
                font-size: 1.4rem;
            }
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body class="admin-body">
    <!-- Barre latérale Prestige -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <h2>Événementia</h2>
        </div>
        <nav class="sidebar-nav">
            <ul>
                <li>
                    <a href="{{ path('admin_dashboard') }}" class="{% if app.request.attributes.get('_route') == 'admin_dashboard' %}active{% endif %}">
                        <i class="fas fa-chart-line"></i>
                        <span>Tableau de bord</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_category_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_category_' %}active{% endif %}">
                        <i class="fas fa-layer-group"></i>
                        <span>Catégories</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_conference_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_conference_' %}active{% endif %}">
                        <i class="fas fa-microphone"></i>
                        <span>Conférences</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_event_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_event_' %}active{% endif %}">
                        <i class="fas fa-calendar-star"></i>
                        <span>Événements</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_lieu_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_lieu_' %}active{% endif %}">
                        <i class="fas fa-map-marker-alt"></i>
                        <span>Lieux</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_reservation_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_reservation_' %}active{% endif %}">
                        <i class="fas fa-ticket-alt"></i>
                        <span>Réservations</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_ticket_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_ticket_' %}active{% endif %}">
                        <i class="fas fa-tags"></i>
                        <span>Tickets</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_user_index') }}" class="{% if app.request.attributes.get('_route') starts with 'app_user_' %}active{% endif %}">
                        <i class="fas fa-users-cog"></i>
                        <span>Utilisateurs</span>
                    </a>
                </li>
            </ul>
        </nav>
    </aside>

    <!-- Contenu principal -->
    <main class="main-content">
        <header class="header">
            <h1>{% block page_title %}Tableau de bord{% endblock %}</h1>
            <div class="user-info">
                <div class="user-avatar">
                    {{ app.user ? app.user.name|slice(0, 1)|upper : 'A' }}
                </div>
                <span class="user-name">{{ app.user ? app.user.name : 'Administrateur' }}</span>
            </div>
        </header>

        <div class="content-wrapper">
            {% block body %}{% endblock %}
        </div>
    </main>

    {% block javascripts %}{% endblock %}
</body>
</html>
