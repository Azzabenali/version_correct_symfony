{{ form_start(form, {'id': 'ticket_form'}) }}
    {{ form_row(form.name) }}
    {{ form_row(form.price) }}
    {{ form_row(form.quantity) }}
    {{ form_row(form.event) }}
    {{ form_row(form.reservation) }}
{{ form_end(form) }}
